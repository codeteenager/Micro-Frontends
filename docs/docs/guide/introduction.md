# 介绍
在我们日常开发中有没有遇到以下问题?
* 不同团队间开发同一应用但是技术栈不同
* 每个团队希望独立开发，独立部署
* 项目中有历史遗留的应用代码，但是又不想再次重新翻写

针对以上问题呢，可以使用微前端来解决。那么什么是微前端？微前端就是将不同的功能按照不同的维度拆分成多个子应用，将子应用打包成一个个的lib，通过主应用来加载这些子应用，当路径切换时加载不同的子应用，这样每个子应用都是独立的，技术栈也不用做限制了，从而解决了前端协同开发的问题。

如果使用iframe，iframe中的子应用切换路由时用户刷新页面会导致状态丢失。
